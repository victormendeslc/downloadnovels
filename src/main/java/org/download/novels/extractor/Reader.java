package org.download.novels.extractor;


public class Reader {
/*
    public Reader(TypeSite codigo, String nomeArquivo, String paginaInicial) throws IOException {
        WebDriverManager.chromedriver().setup();
        switch (codigo) {
            case WUXIAWORLD:
                new Wuxiaworld().execute(nomeArquivo, paginaInicial);
                break;
            case LIGHTNOVEL:
                new LightNovel().execute(nomeArquivo, paginaInicial);
                break;
            case NOVELHALL:
                new NovelHall().execute(nomeArquivo, paginaInicial);
                break;

            default:
                throw new OpcaoException("Opção não encontrada");

        }

    }

 */

}
